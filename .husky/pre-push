#!/bin/sh
# Pre-push: run lint, typecheck, and test before allowing push

echo "Running lint..."
pnpm lint || exit 1

echo "Running typecheck..."
pnpm typecheck || exit 1

echo "Running test..."
pnpm test || exit 1

echo "Lint, typecheck, and test passed. Pushing..."
